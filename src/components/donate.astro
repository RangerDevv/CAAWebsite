<div class="donate-section">
  <div class="container mx-auto px-6 py-20">
    <div class="text-center mb-16">
      <h2 class="text-5xl font-bold mb-4">Support Causes We Care About</h2>
      <p class="text-xl opacity-80">Help us give back to the Central Asian community</p>
    </div>

    <div class="donate-grid">
      <!-- Cause 1 -->
      <div class="donate-card">
        <div class="card-icon">🎓</div>
        <div class="card-content">
          <h3 class="card-title">Education Fund</h3>
          <p class="card-description">Supporting scholarships and educational programs for Central Asian students pursuing higher education</p>
          <div class="impact-badge">
            <span class="impact-number">50+</span>
            <span class="impact-text">Students Helped</span>
          </div>
          <a href="#" class="donate-btn">
            <span>Donate Now</span>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
            </svg>
          </a>
        </div>
      </div>

      <!-- Cause 2 -->
      <div class="donate-card">
        <div class="card-icon">🌍</div>
        <div class="card-content">
          <h3 class="card-title">Community Relief</h3>
          <p class="card-description">Providing humanitarian aid and disaster relief to Central Asian communities in need around the world</p>
          <div class="impact-badge">
            <span class="impact-number">1000+</span>
            <span class="impact-text">Families Supported</span>
          </div>
          <a href="#" class="donate-btn">
            <span>Donate Now</span>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
            </svg>
          </a>
        </div>
      </div>

      <!-- Cause 3 -->
      <div class="donate-card">
        <div class="card-icon">🎨</div>
        <div class="card-content">
          <h3 class="card-title">Cultural Preservation</h3>
          <p class="card-description">Preserving and promoting Central Asian arts, languages, and cultural heritage for future generations</p>
          <div class="impact-badge">
            <span class="impact-number">25+</span>
            <span class="impact-text">Projects Funded</span>
          </div>
          <a href="#" class="donate-btn">
            <span>Donate Now</span>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>

    <div class="info-banner">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="10"></circle>
        <path d="M12 16v-4"></path>
        <path d="M12 8h.01"></path>
      </svg>
      <p>100% of donations go directly to the causes. We cover all processing fees.</p>
    </div>
  </div>
</div>

<style>
  .donate-section {
    padding: 4rem 0;
    position: relative;
    background: radial-gradient(circle at 30% 20%, rgba(88,101,242,0.18), transparent 60%), var(--caa-bg);
  }
  .donate-section::before, .donate-section::after { display: none; }

  .container {
    position: relative;
    z-index: 1;
  }

  .donate-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto 3rem;
  }

  .donate-card {
    background: var(--caa-surface);
    border: 1px solid var(--caa-border);
    border-radius: 1.25rem;
    padding: 0;
    overflow: hidden;
    transition: border-color .3s ease, transform .4s cubic-bezier(.4,0,.2,1), box-shadow .4s;
    box-shadow: 0 4px 18px -2px rgba(0,0,0,.5);
    display: flex;
    flex-direction: column;
  }

  .donate-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 14px 38px -6px rgba(0,0,0,.55);
    border-color: var(--caa-accent);
  }

  .card-icon {
    font-size: 4rem;
    padding: 1.75rem;
    text-align: center;
    background: linear-gradient(135deg, rgba(88,101,242,.15) 0%, rgba(114,137,218,.12) 100%);
    border-bottom: 1px solid var(--caa-border);
    color: var(--caa-accent);
  }

  .card-content {
    padding: 2rem;
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .card-title {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--caa-text);
    margin-bottom: .85rem;
    letter-spacing: .5px;
    transition: color .3s ease;
  }
  .donate-card:hover .card-title { color: var(--caa-accent); }

  .card-description {
    font-size: .95rem;
    line-height: 1.55;
    color: var(--caa-muted);
    margin-bottom: 1.3rem;
    flex: 1;
  }

  .impact-badge {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    background: var(--caa-surface-alt);
    border: 1px solid var(--caa-border);
    border-radius: .85rem;
    padding: .85rem 1.25rem;
    margin-bottom: 1.25rem;
    transition: background .3s ease, border-color .3s ease;
  }
  .donate-card:hover .impact-badge {
    background: rgba(88,101,242,.15);
    border-color: var(--caa-accent);
  }

  .impact-number {
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--caa-accent);
    line-height: 1;
  }

  .impact-text {
    font-size: .65rem;
    color: var(--caa-muted);
    text-transform: uppercase;
    letter-spacing: .12em;
    margin-top: .3rem;
    font-weight: 600;
  }

  .donate-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: .65rem;
    padding: .85rem 1.5rem;
    background: linear-gradient(135deg,var(--caa-accent) 0%,var(--caa-accent-hover) 100%);
    color: #fff;
    border-radius: .65rem;
    font-weight: 600;
    font-size: .9rem;
    letter-spacing: .5px;
    text-decoration: none;
    transition: background .3s ease, transform .3s ease, box-shadow .3s ease;
    box-shadow: 0 4px 16px -2px rgba(88,101,242,.4);
    width: 100%;
  }
  .donate-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 28px -4px rgba(88,101,242,.55);
  }
  .donate-btn:active { transform: translateY(0); }
  .donate-btn:focus-visible { outline: 2px solid var(--caa-accent); outline-offset: 3px; }

  .donate-btn svg { transition: transform .3s ease; }
  .donate-btn:hover svg { transform: scale(1.12); }

  .info-banner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    max-width: 720px;
    margin: 0 auto;
    padding: 1.25rem 1.75rem;
    background: var(--caa-surface);
    border: 1px solid var(--caa-border);
    border-radius: .9rem;
    color: var(--caa-text);
    font-weight: 500;
    font-size: .9rem;
  }

  .info-banner svg { color: var(--caa-accent); flex-shrink: 0; }

  @media (max-width: 768px) {
    .donate-section h2 { font-size: 2.25rem; }
    .donate-grid { grid-template-columns: 1fr; }
    .card-icon { font-size: 3.5rem; padding: 1.4rem; }
    .info-banner { flex-direction: column; text-align: center; }
  }
</style>